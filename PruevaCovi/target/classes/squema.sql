create table usuario (
                         identificacion varchar(15) primary key,
                         telefono varchar(15),
                         nombre varchar(100),
                         apellido varchar(100)
);